apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-charts.fullname" . }}
  labels:
    {{- include "my-app-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-charts.selectorLabels" . | nindent 4 }}
